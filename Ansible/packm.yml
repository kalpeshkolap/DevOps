---
- name: configuring tomcat server
  hosts: test
  become: true
  gather_facts: false
  tasks:
    - name: installing packages
      yum:
        name: '{{ item }}'
        state: present
      loop:
        - git
        - curl
        - wget

